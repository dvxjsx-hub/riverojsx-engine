{
  "name": "riverojsx-engine",
  "version": "1.1.0",
  "private": true,
  "description": "Motor de juego riverojsx-engine — scripts de conveniencia para desarrollo local. El despliegue (Render) sigue apuntando a server/ igual que antes.",
  "scripts": {
    "install:server": "cd server && npm install",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
